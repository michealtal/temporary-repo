// note : if you requir a function in the app.js with out giving the
// value a variable it would still work as long as you are calling a function
// in the code  

const num1 = 5;
const num2= 10;

function addValues() {
    console.log(`the sum is : ${num1 + num2}`)
}

addValues()